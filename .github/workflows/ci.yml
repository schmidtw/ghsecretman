# SPDX-FileCopyrightText: 2022 Comcast Cable Communications Management, LLC
# SPDX-License-Identifier: Apache-2.0
---
name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md
      - CONTRIBUTING.md
      - MAINTAINERS.md
      - LICENSE
      - NOTICE
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  pull-requests: read

jobs:
  ci:
    uses: xmidt-org/shared-go/.github/workflows/ci.yml@bfcee2e5632b9b052e8176a73a9523f226ba7ef4 # v4.12.15
    with:
      release-type:        program
      release-custom-file: true
      yaml-lint-skip:      true
    secrets: inherit
